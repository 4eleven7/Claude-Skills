# Project Agent Guide

Use `AGENTS.md` as the canonical project instruction file.

This file exists only for agent runtimes that load this filename directly. Keep detailed policy in `AGENTS.md` and `Documentation/system/`.
