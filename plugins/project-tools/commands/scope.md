# Scope — Project Intelligence

This Claude command is a thin shortcut for the `project-scope` skill.

Use the `project-scope` skill for this request. Pass `$ARGUMENTS` through as the user-provided scope, target, or instruction.

Do not duplicate workflow logic here. The skill is canonical so the same behavior works in Codex and Claude.
