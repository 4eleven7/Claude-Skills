# Sync Spec — Post-Implementation Reconciliation

This Claude command is a thin shortcut for the `sync-spec` skill.

Use the `sync-spec` skill for this request. Pass `$ARGUMENTS` through as the user-provided scope, target, or instruction.

Do not duplicate workflow logic here. The skill is canonical so the same behavior works in Codex and Claude.
