# Add Competitor — Scout & Document a Competitor App

This Claude command is a thin shortcut for the `competitor-research` skill.

Use the `competitor-research` skill for this request. Pass `$ARGUMENTS` through as the user-provided scope, target, or instruction.

Do not duplicate workflow logic here. The skill is canonical so the same behavior works in Codex and Claude.
