# Implement — Spec-Driven Feature Work

This Claude command is a thin shortcut for the `implement` skill.

Use the `implement` skill for this request. Pass `$ARGUMENTS` through as the user-provided scope, target, or instruction.

Do not duplicate workflow logic here. The skill is canonical so the same behavior works in Codex and Claude.
