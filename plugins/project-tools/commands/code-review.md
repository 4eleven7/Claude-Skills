# Code Review — Fresh-Eyes Critique Loop

This Claude command is a thin shortcut for the `code-review` skill.

Use the `code-review` skill for this request. Pass `$ARGUMENTS` through as the user-provided scope, target, or instruction.

Do not duplicate workflow logic here. The skill is canonical so the same behavior works in Codex and Claude.
