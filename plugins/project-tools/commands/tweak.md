# Tweak — Zero-Ceremony Change

This Claude command is a thin shortcut for the `tweak` skill.

Use the `tweak` skill for this request. Pass `$ARGUMENTS` through as the user-provided scope, target, or instruction.

Do not duplicate workflow logic here. The skill is canonical so the same behavior works in Codex and Claude.
