# Research Questions — Structured Question Generation

This Claude command is a thin shortcut for the `research-questions` skill.

Use the `research-questions` skill for this request. Pass `$ARGUMENTS` through as the user-provided scope, target, or instruction.

Do not duplicate workflow logic here. The skill is canonical so the same behavior works in Codex and Claude.
