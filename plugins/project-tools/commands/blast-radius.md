# Blast Radius — Pre-Ship Diff Analysis

This Claude command is a thin shortcut for the `blast-radius` skill.

Use the `blast-radius` skill for this request. Pass `$ARGUMENTS` through as the user-provided scope, target, or instruction.

Do not duplicate workflow logic here. The skill is canonical so the same behavior works in Codex and Claude.
