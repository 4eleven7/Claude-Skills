# Add Reference — Save External Inspiration

This Claude command is a thin shortcut for the `reference-library` skill.

Use the `reference-library` skill for this request. Pass `$ARGUMENTS` through as the user-provided scope, target, or instruction.

Do not duplicate workflow logic here. The skill is canonical so the same behavior works in Codex and Claude.
